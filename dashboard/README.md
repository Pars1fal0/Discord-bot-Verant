# 📊 Discord Bot Verant - Dashboard

Современный веб-дашборд для визуализации статистики Discord бота Verant.

![Dashboard Preview](preview.png)

## ✨ Возможности

- 📈 **Общая статистика** - пользователи, баланс, бизнесы, игры
- 🏆 **Лидерборды** - топ-10 богачей, уровней и PvP дуэлянтов
- 📊 **Графики** - распределение богатства и уровней
- 💸 **Транзакции** - последние 50 транзакций в реальном времени
- 🔄 **Автообновление** - данные обновляются каждые 30 секунд
- 🎨 **Современный дизайн** - темная тема, градиенты, анимации
- 🌓 **Переключатель темы** - светлая и темная темы с сохранением настроек
- 🔍 **Поиск** - поиск по ID пользователя в лидербордах
- 🎯 **Фильтры** - фильтрация транзакций по типу и поиску

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Discord бот должен быть запущен и создавать JSON файлы

### Установка

1. **Перейдите в директорию dashboard:**
```bash
cd /Users/maxkorostelev/Discord-bot-Verant/dashboard
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Запустите сервер:**
```bash
python dashboard_api.py
```

4. **Откройте в браузере:**
```
http://localhost:5001
```

## 📁 Структура

```
dashboard/
├── dashboard_api.py      # Flask API сервер
├── index.html           # Главная страница
├── style.css            # Стили
├── script.js            # JavaScript логика
├── requirements.txt     # Python зависимости
└── README.md           # Этот файл
```

## 🔌 API Endpoints

Все эндпоинты доступны по адресу `http://localhost:5000/api/`:

- `GET /api/economy` - экономические данные
- `GET /api/levels` - данные уровней
- `GET /api/pvp` - PvP статистика
- `GET /api/business` - бизнес данные
- `GET /api/stocks` - биржа акций
- `GET /api/bank` - банковские счета
- `GET /api/tournaments` - турниры
- `GET /api/enhancements` - улучшения и бустеры
- `GET /api/stats` - агрегированная статистика
- `GET /api/transactions` - последние транзакции

## 🎨 Дизайн

Дашборд использует современный дизайн с:

- **Темная тема** с синей палитрой (#3498db)
- **Glassmorphism** эффекты
- **Плавные анимации** и переходы
- **Адаптивный дизайн** (mobile-first)
- **Chart.js** для графиков

### Цветовая палитра

- 🔵 Primary: `#3498db`
- 💙 Accent: `#5dade2`
- ✅ Success: `#2ecc71`
- 🌑 Background: `#0f1419`

## ⚙️ Конфигурация

### Изменение порта

По умолчанию сервер запускается на порту 5000. Чтобы изменить порт, отредактируйте `dashboard_api.py`:

```python
app.run(debug=True, host='0.0.0.0', port=5000)  # Измените 5000 на нужный порт
```

### Изменение интервала обновления

По умолчанию данные обновляются каждые 30 секунд. Чтобы изменить интервал, отредактируйте `script.js`:

```javascript
setInterval(refreshAll, 30000);  // 30000 = 30 секунд
```

## 🔧 Разработка

### Запуск в режиме разработки

```bash
python dashboard_api.py
```

Flask автоматически перезагрузится при изменении Python файлов.

### Отладка

Откройте консоль разработчика в браузере (F12) для просмотра логов и ошибок.

## 📝 Примечания

- Дашборд работает только для чтения - он не может изменять данные бота
- Данные берутся из JSON файлов в корневой директории бота
- Убедитесь, что бот запущен и создает JSON файлы
- Для продакшена рекомендуется использовать WSGI сервер (gunicorn, uwsgi)

## 🐛 Устранение неполадок

### Дашборд не показывает данные

1. Убедитесь, что бот запущен и создал JSON файлы
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что Flask сервер запущен

### Ошибка CORS

Если вы запускаете дашборд на другом домене, убедитесь, что CORS настроен правильно в `dashboard_api.py`.

### Графики не отображаются

Убедитесь, что Chart.js загружается корректно. Проверьте подключение к интернету.

## 📄 Лицензия

MIT License - см. LICENSE в корневой директории проекта

## 🙏 Благодарности

- Flask за отличный веб-фреймворк
- Chart.js за красивые графики
- Inter font за современную типографику

---

Made with ❤️ and ☕
